/**
 * @deprecated Use src/agents/definitions/profiles.ts directly.
 * This file is kept as a backward-compatible re-export shim so existing
 * import paths continue to resolve without churn in every consumer.
 */
export type { AgentCapabilities, AgentProfile } from '../agents/definitions/profiles.js';
export { getAgentProfile } from '../agents/definitions/profiles.js';
