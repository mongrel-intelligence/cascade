## Git and GitHub CLI

Use standard git commands and GitHub CLI (`gh`) via Tmux for all version control.

**CRITICAL: Pass commands as argv arrays to Tmux, NOT shell strings!**

**Git command examples:**
- Branch: `command=["git", "checkout", "-b", "feature/name"]`
- Stage: `command=["git", "add", "."]`
- Commit: `command=["git", "commit", "-m", "fix(auth): correct validation"]`
- Push: `command=["git", "push", "-u", "origin", "HEAD"]`

**GitHub CLI (`gh`) examples:**
- Create PR: `command=["gh", "pr", "create", "--title", "Fix auth bug", "--body", "Details..."]`
- View PR: `command=["gh", "pr", "view"]`
- List PRs: `command=["gh", "pr", "list"]`

**IMPORTANT:**
- Always provide `--title` and `--body` to avoid interactive prompts. Do NOT use `--fill`
- Each array element is a separate argument - NO quotes needed in array elements
- Multiline bodies work fine: `"## Summary\n\nDetails here"`

## Commit Message Format

Use conventional commits:
- feat(scope): new feature
- fix(scope): bug fix
- refactor(scope): code refactoring
- test(scope): adding tests
- docs(scope): documentation
- chore(scope): maintenance

Example: `feat(auth): add user authentication endpoint`
