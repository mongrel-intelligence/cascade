## Running Commands

Use the Tmux gadget for ALL shell commands:

1. **Start command:** `Tmux(action="start", session="unique-name", command="pnpm run test:unit")`
   - Waits up to 120s for initial output
   - If process exits quickly: returns full output + exit status
   - If still running: returns partial output + "still running" message

2. **Check output:** `Tmux(action="capture", session="unique-name")`
   - Get recent output from a running session

3. **Stop/cleanup:** `Tmux(action="kill", session="unique-name")`
   - Terminate the session when done (always clean up!)

**Command Format:** Pass command as a shell string. Pipes, &&, ||, redirects, and globs all work:
- Simple: `command="npm test"`
- Chained: `command="npm run lint && npm test"`
- With pipes: `command="npm test 2>&1 | head -50"`
- With globs: `command="find . -name '*.ts' | xargs wc -l"`

Use unique session names like: "npm-install", "test-run", "lint-check", "build", "typecheck"
