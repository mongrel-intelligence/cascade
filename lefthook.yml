pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: npx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: npx tsc --noEmit

pre-push:
  parallel: true
  commands:
    test:
      run: npm run test:fast

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
