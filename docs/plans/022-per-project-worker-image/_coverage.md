# Coverage map for spec 022-per-project-worker-image

Auto-generated by /plan. Tracks which plans satisfy which spec ACs.

## Spec ACs

| # | Spec AC (short) | Satisfied by | Status |
|---|---|---|---|
| 1 | Unconfigured project spawns exactly as today | plan 2 (spawn-resolution) | full |
| 2 | Set/clear round-trips through CLI + API + dashboard | plan 1 (schema, storage) + plan 3 (CLI/API) + plan 4 (dashboard) | partial chain |
| 3 | Verified image pinned to a digest; launches use the digest | plan 3 (resolve + pin) + plan 2 (launch uses digest) | partial chain |
| 4 | Fail-closed validation; pending/failed never launches; precise reason | plan 3 (validate + state) + plan 2 (spawn refuses unverified) | partial chain |
| 5 | Spawn from configured image; log shows which image won | plan 2 (spawn-resolution) | full |
| 6 | Missing-pullable → pull+retry; unobtainable/invalid → fail loud | plan 2 (spawn-resolution) | full |
| 7 | Snapshot coexistence + correct reuse classification | plan 2 (spawn-resolution) | full |
| 8 | Only superadmin may set/change; recorded for audit | plan 3 (set-validation) | full |
| 9 | Launch posture unchanged (no mounts, no privileged) | plan 2 (spawn-resolution) | full |
| 10 | Operator docs walkthrough, both topologies `[manual]` | plan 4 (ui) | full (manual) |

## Coverage summary

- **10 spec ACs** mapped to **4 plans** — every AC covered (no drops).
- **6 full-coverage ACs**: #1, #5, #6, #7, #9 (plan 2), #8 (plan 3), #10 (plan 4, manual).
- **3 partial-chain ACs**: #2 (plan 1 + 3 + 4), #3 (plan 3 + 2), #4 (plan 3 + 2) — done only when all plans in the chain are done.
- **1 `[manual]` AC**: #10 — operator-walkthrough documentation check (protocol in plan 4); the *behavior* it
  documents (a custom image runs; a broken one is rejected) is auto-tested under ACs #4–#7.

## Plan dependency graph

```
1-schema ──→ 2-spawn-resolution ──→ 3-set-validation ──→ 4-ui
```

Topological order: schema → spawn-resolution → set-validation → ui (linear, no cycles).

## Notes

- **Linear (not parallel)** on purpose: plans 2 and 3 are technically independent off plan 1, but sequencing
  spawn-resolution (2) before set-validation (3) avoids a "configured-but-ignored" intermediate state — by the
  time an operator can set an image, spawn already honors it.
- The load-bearing, non-additive change is the `effectiveBaseImage` correctness fix in plan 2 (rewriting the
  three "is this the base image?" sentinel sites). Its first task is a behavior-preserving refactor pinned by
  the AC #1 regression test.
- Validation runs **router-side** (Docker socket is router-only) as an eager job on the existing dashboard-jobs
  queue — see plan 3.
