# Coverage map for spec 024-shared-board-repo-topologies

Auto-generated by /plan. Tracks which plans satisfy which spec ACs.

## Spec ACs

| # | Spec AC (short) | Satisfied by | Status |
|---|---|---|---|
| 1 | Duplicate-repo save accepted under primary rule / rejected with named conflict, never generic error | plan 1 (schema-and-resolver-seam: column + DB constraint) + plan 4 (github-link-first-routing: behavior) | partial chain |
| 2 | Shared-key event routes to the discriminated sibling | plan 1 (resolver logic) + plan 2 (jira-event-routing: wiring) | partial chain |
| 3 | No-match event routes to the discriminator-less default sibling | plan 1 + plan 2 | partial chain |
| 4 | No-match without default skipped with reason naming key + discriminators | plan 1 + plan 2 | partial chain |
| 5 | Ambiguous match skipped with reason + observability capture | plan 1 + plan 2 | partial chain |
| 6 | Scoped reads return only own-discriminator items | plan 3 (jira-scope-and-stamp) | full |
| 7 | Created items carry the discriminator and route back | plan 3 (stamping) + plan 2 (routing of stamped items) | partial chain |
| 8 | Linked-PR events route to the linked project regardless of sharing | plan 1 (link lookup) + plan 4 (wiring) | partial chain |
| 9 | Unlinked events on shared repos route to the primary project | plan 1 + plan 4 | partial chain |
| 10 | Exactly one primary per repo enforced with clear errors | plan 1 (DB partial unique) + plan 4 (friendly validation) | partial chain |
| 11 | Duplicate key without (distinct) discriminator rejected at save | plan 2 | full |
| 12 | No-sharing deployments byte-identical in routing, config requirements, and log reasons | plans 1+2+3+4+5 (explicit pin tests in each) | partial chain |
| 13 | Discriminator + primary configurable in the dashboard | plan 5 (wizard-config-ui) | full |

## Coverage summary

- **13 spec ACs** mapped to **5 plans**
- **3 plans** with full-coverage ACs (plan 2 → AC 11; plan 3 → AC 6; plan 5 → AC 13)
- **5 plans** with partial-coverage ACs (schema/behavior and logic/wiring splits; AC 12 is a cross-cutting regression pin carried by every plan)

## Plan dependency graph

```
1-schema-and-resolver-seam ──→ 2-jira-event-routing ──────────→ 5-wizard-config-ui
             ├───────────────→ 3-jira-scope-and-stamp            ↗
             └───────────────→ 4-github-link-first-routing ─────┘
```

Topological order: 1 → {2, 3, 4 in parallel} → 5 (5 needs 2+4; 3 may still be in flight).
File-disjointness: 1 pre-owns `src/config/provider.ts`/`configCache.ts` + repositories; 2 owns `src/router/adapters/jira.ts` + `src/db/repositories/integrationsRepository.ts`; 3 owns `src/pm/jira/adapter.ts` + `src/jira/client.ts`; 4 owns `src/router/adapters/github.ts` + `src/api/routers/projects.ts`; 5 owns the web wizard/SCM-tab files + `src/integrations/pm/jira/manifest.ts`. Shared-append surface: `src/integrations/README.md` (plans 2, 3, 5 — self-contained blocks).
