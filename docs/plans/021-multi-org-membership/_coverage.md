# Coverage map for spec 021-multi-org-membership

Auto-generated by /plan. Tracks which plans satisfy which spec ACs.

## Spec ACs

| # | Spec AC (short) | Satisfied by | Status |
|---|---|---|---|
| 1 | Admin adds an existing account to their org → it gains access | plan 3 (management) — API/CLI; plan 4 (ui) — dashboard form | full (management) |
| 2 | Duplicate-email create never 500s; graceful, actionable result | plan 3 (management) | full |
| 3 | Multi-org user enumerates orgs + selects active; data scoped | plan 2 (access) — list/set/scope + plan 4 (ui) — switcher | partial chain |
| 4 | Per-org role governs permissions (admin here, member there) | plan 2 (access) | full |
| 5 | Org member list shows every member (incl. cross-home) | plan 3 (management) — listing API + plan 4 (ui) — render | partial chain |
| 6 | Migration: one backfilled membership/user; stays logged in; same default org | plan 1 (schema) | full |
| 7 | Superadmin cross-org access unchanged | plan 2 (access) | full |
| 8 | An org admin cannot view/grant another org; only superadmin cross-org | plan 2 (access) | full |
| 9 | Switcher visible for multi-org, hidden/inert for single-org `[manual]` | plan 4 (ui) | full (manual) |

## Coverage summary

- **9 spec ACs** mapped to **4 plans** — every AC covered (no drops).
- **7 full-coverage ACs**: #1 (management), #2 (management), #4 (access), #6 (schema), #7 (access), #8 (access), #9 (ui, manual).
- **2 partial-chain ACs**: #3 (access + ui), #5 (management + ui) — done only when both plans in the chain are done.
- **1 `[manual]` AC**: #9 — visual presence/placement of the switcher (protocol in plan 4); its scoping behavior is auto-tested under AC #3.

## Plan dependency graph

```
1-schema ──→ 2-access ──→ 3-management ──→ 4-ui
```

Topological order: schema → access → management → ui (linear, no cycles).
