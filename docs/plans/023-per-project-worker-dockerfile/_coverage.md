# Coverage map for spec 023-per-project-worker-dockerfile

Auto-generated by /plan. Tracks which plans satisfy which spec ACs.

## Spec ACs

| # | Spec AC (short) | Satisfied by | Status |
|---|---|---|---|
| 1 | Unconfigured **and** referenced projects spawn exactly as today (regression pin) | plan 2 (spawn-resolution) | full |
| 2 | Set/clear round-trips through CLI + API + dashboard | plan 1 (schema/storage) + plan 4 (CLI/API) + plan 5 (dashboard) | partial chain |
| 3 | Exactly one effective image source; mutual exclusivity | plan 1 (derived source) + plan 4 (enforce both directions) + plan 5 (UX) | partial chain (enforced in plan 4) |
| 4 | Reject content declaring its own base at set time; nothing persisted | plan 4 (set-surfaces) | full |
| 5 | Compose + build + validate fail-closed; build-fail vs runtime-missing distinct; pending/building/failed never launches | plan 3 (build + fail-closed record) + plan 2 (spawn gate) | full (build in 3, launch-gate in 2) |
| 6 | Async build: building → verified/failed; supersede; wall-clock timeout, never wedged | plan 3 (job/states/supersede/timeout) + plan 4 (write building + API) + plan 5 (render) | partial chain (core in 3) |
| 7 | Spawn launches the built image; log shows which image + Dockerfile origin | plan 2 (spawn-resolution) | full |
| 8 | Content-hash rebuild/reuse; explicit rebuild | plan 3 (engine reuse) + plan 4 (idempotent skip + rebuild trigger) + plan 5 (button) | partial chain |
| 9 | GC + last-good pin; a failed rebuild or GC sweep never strands a project | plan 3 (build-engine) | full |
| 10 | Superadmin-only + audited | plan 4 (set-surfaces) | full |
| 11 | Launch posture unchanged; build gated + bounded + no secrets; master key absent from build | plan 2 (launch posture) + plan 3 (build no-secrets/timeout) + plan 4 (superadmin gate) | partial chain |
| 12 | Single-daemon reachability guard — fail loud, never silent fallback | plan 2 (spawn-resolution) | full |
| 13 | Operator docs walkthrough `[manual]` | plan 5 (ui) | full (manual) |

## Coverage summary

- **13 spec ACs** mapped to **5 plans**
- **7 spec ACs** with full coverage from a single plan (#1, #4, #7, #9, #10, #12, #13)
- **1 spec AC** (#5) fully covered but split across two plans (build in 3, launch-gate in 2)
- **5 spec ACs** with partial-chain coverage requiring downstream plans to complete (#2, #3, #6, #8, #11)
- **1 `[manual]` AC** (#13) — operator-docs walkthrough, verified via plan 5's manual protocol
- **0 spec ACs** unmapped

## Plan dependency graph

```
1-schema ──→ 2-spawn-resolution ──→ 3-build-engine ──→ 4-set-surfaces ──→ 5-ui
```

Linear DAG, no cycles. Topological order = plan order (1 → 2 → 3 → 4 → 5).

## Notes

- **Schema reuse:** the four spec-022 `worker_image*` columns are reused (not duplicated). `worker_image_digest`
  holds the active launchable pin for **both** sources (registry digest for a referenced image, immutable local
  image ID for a built one). Three new columns are added by plan 1: `worker_dockerfile`,
  `worker_image_build_hash` (content-hash — column/job identity + supersede guard), and `worker_image_build_status`
  (build-attempt status, split from `worker_image_status` so a failed rebuild can't strand a running project).
- **Two hashes:** the Docker-free **content-hash** (set surface, plan 4 — column/job identity) vs the
  **full hash** = `sha256(composed + base digest)` (build engine, plan 3 — image label for base-drift-aware reuse).
- **GC reuse:** built images carry `LABEL cascade.managed=true` and a stable per-project tag, so the existing
  30-min dangling-image reaper reclaims superseded digests while the active tagged image is structurally exempt.
- **Non-standard-spec note:** n/a — the spec follows the /specify template.
